{
  "name": "photos.vassopoli.com",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env scripts/fetch-photos.mjs && astro dev",
    "build": "node --env-file-if-exists=.env scripts/fetch-photos.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.9"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "exifr": "^7.1.3"
  }
}